import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const primary = Color(0xFF2E7D5B); // trust/service green
  static const primaryDark = Color(0xFF1B5E3F);
  static const accent = Color(0xFFE8A33D); // warm accent for CTAs
  static const background = Color(0xFFF7F8FA);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF1A1D1F);
  static const textSecondary = Color(0xFF6B7280);
  static const error = Color(0xFFE5484D);

  // Booking status colors
  static const statusPending = Color(0xFFF5A623);
  static const statusConfirmed = Color(0xFF2E7D5B);
  static const statusCompleted = Color(0xFF6B7280);
  static const statusCancelled = Color(0xFFE5484D);
}
